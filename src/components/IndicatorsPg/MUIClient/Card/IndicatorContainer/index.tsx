import Box from "@mui/material/Box";

import styles from "../../../styles.module.scss";

function IndicatorContainer({ children }) {
  return <Box className="indicatorContainer">{children}</Box>;
}

export default IndicatorContainer;
